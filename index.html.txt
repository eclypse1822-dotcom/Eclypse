<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>ECLYPSE</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background: black;
      color: white;
    }
    header {
      text-align: center;
      padding: 40px 20px;
    }
    header img {
      width: 200px;
      max-width: 80%;
    }
    section {
      padding: 40px 20px;
      max-width: 1100px;
      margin: auto;
    }
    h2 {
      text-align: center;
      margin-bottom: 30px;
      letter-spacing: 2px;
    }
    .product, .lookbook {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 20px;
    }
    .card {
      background: #111;
      padding: 20px;
      border: 1px solid #222;
      text-align: center;
    }
    .card img {
      width: 100%;
    }
    .soldout {
      color: red;
      font-weight: bold;
      margin-top: 10px;
    }
    .countdown {
      text-align: center;
      font-size: 20px;
      margin-top: 20px;
    }
    footer {
      text-align: center;
      padding: 30px;
      font-size: 14px;
      opacity: 0.6;
    }
    button {
      background: white;
      color: black;
      border: none;
      padding: 12px 20px;
      margin-top: 15px;
      cursor: not-allowed;
      opacity: 0.5;
    }
  </style>
</head>
<body>

<header>
  <!-- AQUÍ VA TU LOGO -->
  <img src="logo.png" alt="Eclypse Logo">
  <div class="countdown" id="countdown"></div>
</header>

<section>
  <h2>DROP 001</h2>
  <div class="product">
    <div class="card">
      <!-- AQUÍ VA LA CAMISA -->
      <img src="shirt.png" alt="Eclypse Tee">
      <p>ECLYPSE TEE</p>
      <p class="soldout">SOLD OUT</p>
      <button>AGOTADO</button>
    </div>
  </div>
</section>

<section>
  <h2>LOOKBOOK</h2>
  <div class="lookbook">
    <div class="card"><img src="look1.png"></div>
    <div class="card"><img src="look2.png"></div>
  </div>
</section>

<footer>
  © ECLYPSE CLOTHING CO.
</footer>

<script>
  // FECHA DEL DROP (CAMBIA CUANDO QUIERAS)
  const dropDate = new Date("2026-01-20T18:00:00").getTime();

  const timer = setInterval(() => {
    const now = new Date().getTime();
    const diff = dropDate - now;

    if (diff < 0) {
      document.getElementById("countdown").innerHTML = "DROP LIVE";
      clearInterval(timer);
      return;
    }

    const d = Math.floor(diff / (1000 * 60 * 60 * 24));
    const h = Math.floor((diff / (1000 * 60 * 60)) % 24);
    const m = Math.floor((diff / (1000 * 60)) % 60);
    const s = Math.floor((diff / 1000) % 60);

    document.getElementById("countdown").innerHTML =
      `DROP EN ${d}D ${h}H ${m}M ${s}S`;
  }, 1000);
</script>

</body>
</html>
